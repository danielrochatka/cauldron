[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "cauldron-ai-attachments"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "cauldron>=0.1.0",
    "django>=4.2",
    "pypdf>=4.0",
    "python-docx>=1.1",
]

[project.optional-dependencies]
ai = [
    "cauldron-ai>=0.1.0",
    "cauldron-ai-admin>=0.1.0",
]
test = [
    "cauldron-ai-admin>=0.1.0",
    "cauldron-ai>=0.1.0",
    "pytest>=8.0",
    "pytest-django>=4.8",
]

[project.entry-points."cauldron.modules"]
"cauldron.ai.attachments" = "cauldron_ai_attachments.module:module"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
cauldron_ai_attachments = ["migrations/*.py"]

[tool.pytest.ini_options]
pythonpath = ["src", "tests", "../../src", "../.."]
addopts = "-ra"
testpaths = ["tests"]

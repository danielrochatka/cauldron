[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "cauldron-content-operations"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "cauldron>=0.1.0",
    "cauldron-content>=0.1.0",
    "cauldron-workspace-flatfile>=0.1.0",
    "cauldron-django-state>=0.1.0",
    "cauldron-django-auth>=0.1.0",
    "django>=4.2",
]

[project.entry-points."cauldron.modules"]
"cauldron.content.operations" = "cauldron_content_operations.module:module"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src", "../../src", "../.."]
addopts = "-ra"
testpaths = ["tests"]

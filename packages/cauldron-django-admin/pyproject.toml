[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "cauldron-django-admin"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["cauldron>=0.1.0", "cauldron-django-auth>=0.1.0"]

[project.entry-points."cauldron.modules"]
"cauldron.django.admin" = "cauldron_django_admin.module:module"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src", "../../src", "../.."]
addopts = "-ra"
